extend layout

append styles
  link(rel='stylesheet', href='_/register.css')

block content
  form(method='post', action='join').text-center: .container
    h3.mb-4 הרשמה לטריוויית יום העלמות סטושי
    //-.input-group.mt-5
      input.form-control.form-control-lg(name='name', type='text', placeholder='שם/כינוי', required, minlength=3, value=(username || ''))
      .input-group-append: button.btn-lg.btn-primary(type='submit') הרשמה
    input.form-control.form-control-lg(name='name', type='text', placeholder='שם/כינוי', required, minlength=3, maxlength=20, value=(username || ''))
    button.btn-lg.btn-primary.btn-block.mt-2(type='submit') הרשמה
    h4.mt-4 שגרירות הביטקוין תל אביב